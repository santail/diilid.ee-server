var fs = require('fs');



var pakkumised = (function() {
    var pakkumised = {};

    pakkumised.parse = function($, site) {
        eval(fs.readFileSync(__dirname + '/models/' + site + '.js', 'utf8') + '')
        return template
    }

    return pakkumised;
})();

module.exports = pakkumised;